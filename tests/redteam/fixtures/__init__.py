"""Sealed, committed adversarial corpora for red-team probes.

Corpora are JSONL files committed verbatim. The sibling ``generate_*.py`` scripts
exist for reproducibility/audit only — they are not invoked at probe runtime.
"""
